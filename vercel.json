{
    "builds": [{
        "src": "djangomediapro/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "djangomediapro/wsgi.py"
        }
    ]
}